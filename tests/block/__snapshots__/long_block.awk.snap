{
    if (first_session_start != last_session_end) { first_session_start = lib_add_time(first_session_start, "-60m") }
}
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
{
    if (first_session_start != last_session_end) {
        first_session_start = lib_add_time(first_session_start, "-60m")
    }
}
